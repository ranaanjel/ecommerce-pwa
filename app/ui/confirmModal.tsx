import { Dispatch, SetStateAction } from "react"

export function ConfirmModal({openModal, setConfirmDelete} : {
    openModal:boolean, setConfirmDelete: Dispatch<SetStateAction<boolean>>
}) {

    return <div className="h-screen overflow-hidden bg-gray-100 w-screen absolute">

    </div>

}